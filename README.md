# homebrew-tap

Homebrew tap for [credctl](https://github.com/credctl/credctl) — machine identity management with the macOS Secure Enclave.

## Install

```bash
brew tap credctl/tap
brew install --cask credctl
```

## Update

```bash
brew upgrade --cask credctl
```
